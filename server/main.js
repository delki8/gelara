import { Meteor } from 'meteor/meteor';
import '/imports/api/musics';
import '/imports/api/sundays';

Meteor.startup(() => {
  // const data = fs.readFileSync("/home/ruither/proxies.txt", "utf8");
  // console.log(data);/
});
